[project]
name = "ticketeer"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "openai>=1.0",
    "numpy>=2.0",
    "pyyaml>=6.0",
    "pydantic>=2.0",
]

[project.scripts]
ticketeer = "ticketeer.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
